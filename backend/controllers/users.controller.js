export const createUser = (req, res) => {
  res.json("creando usuario");
};

export const loginUser = (req, res) => {
  res.json("login");
};

export const misDatos = (req, res) => {
  res.json("mis datos");
};
